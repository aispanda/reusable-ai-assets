# Issue-resolution patterns

Record only failures that improve this reusable workflow. Keep raw logs out; retain the smallest evidence needed to reproduce and prevent recurrence.

| ID | Trigger / symptom | Root cause and evidence | Safe resolution | Preventive improvement | Verification | Status |
|---|---|---|---|---|---|---|
| AR-001 | `scaffold_asset.py` raised `PermissionError` when creating an asset in the central library | The central library was outside the active project's normal write boundary; the failure occurred before an asset folder or inventory row was created | Confirm the exact central target, then rerun only the scaffold command with scoped write authorization | Scaffold now returns a concise permission instruction and rolls back a newly created folder if a later write fails; workflow requires scoped approval for external canonical libraries | RA-004 scaffold succeeded with scoped authorization; smoke test, skill validation and central library validation passed | Resolved 2026-08-06 |
| AR-002 | Standard skill validation failed first with missing `yaml`, then with a Windows character-decoding error | The bundled Python lacked PyYAML; system Python had it but defaulted to a non-UTF-8 locale while the skill used valid Unicode punctuation | Use an environment containing the validator dependency and enable Python UTF-8 mode for that command | Validation instructions should declare the PyYAML dependency and use UTF-8 explicitly on Windows; asset scripts themselves remain standard-library-only | `PYTHONUTF8=1` with system Python returned `Skill is valid!` for RA-005 | Resolved 2026-08-06 |
| AR-003 | Running an asset’s audit tests left `scripts/__pycache__` inside the canonical transferable package | The audit imported a sibling module with normal bytecode caching enabled; the package manifest exposed the generated `.pyc` after validation | Remove the exact generated cache and disable bytecode writes before local helper imports | Reusable Python validation tools that import sibling scripts should set `sys.dont_write_bytecode = True` or run with `PYTHONDONTWRITEBYTECODE=1`; always inspect the final transfer manifest after tests | RA-005 tests passed after the change and a final recursive file check found no cache artifact | Resolved 2026-08-06 |

| AR-004 | A new-asset scaffold reported that the requested ID was already registered | The capability already existed under an established asset name; creating another folder would split ownership and make inventory discovery ambiguous | Stop scaffolding, read the existing `ASSET.md` and entry skill, then enhance that asset in place while preserving its stable ID and boundary | Before assigning an ID or folder, search the central inventory by capability, keywords and adjacent assets; prefer versioned enhancement over a competing asset | RA-006 retained its stable identity and received website-strategy, ecosystem and profile-governance additions without a duplicate inventory row | Resolved 2026-08-06 |
| AR-005 | A library marked reusable still contained another project's names, configuration, schema fixture and machine paths | Structural validation checked manifests but not public content boundaries; several examples were copied from working projects | Replace project material with fictional profiles, parameterize machine paths and quarantine private evidence outside the transferable package | Public validation now supports strict machine-path, secret-pattern and repeatable forbidden-term checks; require it on a staged package before publication | Contamination fixture fails the validator; RA-010 and RA-011 path examples were generalized and the full working-library scan passes | Reinforced 2026-08-11 |
| AR-006 | An AI utility loaded credentials from one hard-coded personal `.env` location | Convenience configuration crossed the reusable-core boundary and made the asset machine-specific | Read standard environment variables or an ignored project-local `.env`; production consumers use an approved secret manager | Strict validation rejects personal machine paths and credential-like content; quality gate requires replaceable configuration | RA-011's credential-like client value was removed from source and replaced by runtime configuration; the full working-library scan passes | Reinforced 2026-08-11 |
| AR-007 | A central working library contained dependency folders, generated builds and Python bytecode beside reusable source | Local execution output was not separated from the transfer package | Keep working output unpublishable and construct publication from a clean staged copy; remove generated output only with explicit scope | Root ignore policy plus strict staged-package validation reject dependency, generated and bytecode paths | Recurrence detected by a working-root strict scan; clean staging remains the mandatory publication boundary | Reinforced 2026-08-07 |
| AR-008 | Validation could not start because `python` was not on the operator's `PATH` | Documentation assumed a global runtime instead of resolving the approved project/workspace runtime | Resolve a configured Python executable during preflight; do not install or hard-code one machine's runtime into the asset | Publication workflow records the runtime/version used, while examples use a configurable `python` placeholder | Smoke test passed with the approved workspace runtime | Resolved 2026-08-06 |
| AR-009 | Publication checks depended on repeatedly typing private project terms and local exceptions | The reusable validator had secure generic defaults but no persistent project-owned policy contract | Add a JSON `--policy` interface for forbidden terms, extra secret/path patterns, credential filenames and generated folders; keep one-off CLI overrides | Asset code contains no client-specific rules; each consumer owns its policy file and can version a sanitized policy or keep a private one outside the public package | Clean and contaminated policy-driven fixtures pass/fail as expected | Resolved 2026-08-06 |
| AR-010 | The generic skill-creator checker could not run in the selected bundled runtime | That checker imports PyYAML, which was absent; installing packages merely for validation would add environment drift | Run the asset's dependency-free smoke tests and direct frontmatter/line-limit checks; use the generic checker only from an approved runtime that already supplies its dependencies | Validators must declare or avoid runtime dependencies; never hard-code a personal interpreter path into the asset | Policy smoke test and direct skill-structure validation passed | Resolved 2026-08-06 |
| AR-011 | A Bash-based asset test failed from PowerShell because the located shell lacked Unix utilities on its non-login path | Finding an executable did not prove that its expected runtime environment was initialized | Invoke the approved shell as a login environment, or ask the operator to run the command from that shell's own terminal; never install a replacement implicitly | Runtime preflight verifies one representative utility as well as the shell version before running the suite | Deployment test passed through Git Bash login mode | Resolved 2026-08-06 |
| AR-012 | A validated public package failed its commit gate on trailing spaces and repeated blank lines at end-of-file | Content and portability checks did not include repository whitespace rules | Normalize publishable text mechanically before staging; preserve content and rerun functional tests afterward | Publication workflow includes `git diff --cached --check` after staging and before commit | Canonical and staged text normalized; commit gate rerun | Resolved 2026-08-06 |
| AR-013 | A clean approved commit could not be pushed because Git used cached credentials for an unrelated account | Commit author configuration was verified, but the independent remote-authentication principal was not | Before committing, compare the target repository owner with the active Git credential account and confirm write access; switch accounts through the approved credential manager when mismatched | Public-source gate records remote URL, visibility, commit author and authenticated principal separately | Push failed safely with HTTP 403 before publication | Resolved 2026-08-06 |
| AR-014 | `UnicodeEncodeError: 'charmap' codec can't encode characters` when printing Indic text to stdout on Windows | System Python defaults to `cp1252` encoding instead of UTF-8 when piping or printing output on Windows | Set `PYTHONIOENCODING=utf-8` environment variable for the script | Always wrap subprocess calls that process Indic text with an environment copy that explicitly forces `PYTHONIOENCODING='utf-8'` | `scratch_batch_process.py` ran successfully on Gujarati transcripts | Resolved 2026-08-07 |
| AR-015 | Sarvam API returning `404 Not Found` when using litellm with `openai/` custom provider base | Sarvam's endpoints differ from the OpenAI spec and require Litellm's native `sarvam/` provider logic to construct correct requests | Route using the model string `sarvam/<model-name>` and expose `SARVAM_API_KEY` to the environment | Review provider-specific Litellm documentation for supported native prefixes instead of falling back to generic OpenAI wrappers | `test_sarvam_router.py` correctly parsed and routed to Sarvam | Resolved 2026-08-07 |
| AR-016 | A newly delivered project followed handover discipline operationally but lacked project-owned protocol and live-state files | Cross-IDE continuity was treated as closing documentation instead of a scaffolded baseline capability | Enhance the existing delivery-governance asset rather than create a duplicate; scaffold stable protocol/live handover and audit their freshness and state fields | During asset adoption, map every recurring governance behavior to a generated artifact and deterministic audit before calling the transfer complete | Rapid Solution Delivery Kit owns the cross-IDE handover module, templates and validation | Resolved 2026-08-07 |
| AR-017 | A broad multi-file patch failed before applying because copied console text did not match the source file's actual Unicode bytes | Display encoding obscured punctuation and made a large exact-context patch fragile | Patch small independently verifiable sections using stable ASCII anchors; inspect actual source when an anchor fails | Asset maintenance uses narrow patches, immediate diff checks and functional validation before batching router/inventory updates | Handover enhancement resumed with narrow patches and preserved source encoding | Resolved 2026-08-07 |
| AR-018 | A reusable engineering-principles template preserved generic delivery hygiene but lost the distinctive philosophy proven in a consuming project | Extraction over-compressed high-value principles and made the reusable template too shallow | Generalize the philosophy without project/vendor names; add a reusable research brief, scaffold template and regression assertions | Asset review compares reusable templates with source-project principles for omitted transferable value while still removing project facts | RA-005 now owns vendor-neutral AI-Age principles and RA-003 owns the compounding skill-improvement loop | Resolved 2026-08-07 |
| AR-019 | Brand/identity flow work produced private employment, legal, spiritual-provenance and unreleased positioning detail that must not enter GitHub or the reusable library | Project profile was mixed with method extraction; personal guardrails looked “useful” to copy into the central asset | Keep method only in RA-005/RA-006; gitignore project transcripts (`SEED-*-brand*`, vibe briefs, drafts, `docs/private/`); scrub public registers to generic rows; never package IP/employment/visa/spiritual provenance into reusable core | Before upgrading brand-related assets, run a personal-detail scan: forbid employment/visa/lawsuit/scripture-provenance claims in publishable files; prefer enhance RA-005+RA-006 over a new brand RA unless brand-only engagements repeatedly need a standalone package | First consumer private paths gitignored; RA-005 brand-voice flow + RA-006 emotion-to-interface upgraded with generic entity/commercial/privacy rules only | Resolved 2026-08-08 |
| AR-020 | Next-site brand visuals re-litigated identity and burned ChatGPT/agent tokens without a reusable fast path | Method lived only in one project chat; RA-006 had contract but not load-order + prompt-pack hygiene | Enrich RA-006 with `brand-visual-fast-path.md` + `CHATGPT_IMAGE_PROMPT_PACK.template.md`; RA-005 After-flow points to it; WEB-031 documents anti-thrash | Agents must follow inventory → ASSET → fast-path only; copy template into project; do not invent a new brand RA | Implemented 2026-08-08 |
| AR-006 (recurrence) | Pre-push scan found a utility loading credentials from a fixed personal `.env` path | Same root cause as AR-006: a convenience edit reintroduced a machine-specific credential path into reusable core | Revert to project-local `load_dotenv()`; do not push until the path is gone from the commit that would be published | Before every library push, run a personal-path / personal-project / email scan on the exact commit about to ship; treat a hit as a hard stop | Path removed from working tree and scrubbed from the unpushed commit before push | Reinforced 2026-08-08 |
| AR-020 | Pre-push scan found a personal-project identifier cited inside reusable writing-craft documentation | Adjacent-project provenance was left as an `OBSERVED` citation instead of a topic-neutral evidence label | Replace with generic multimedia-learning provenance; never name private/adjacent personal projects in publishable reusable docs | Extend the public-boundary scan (and publication checklist) to forbid known personal-project tokens and real email addresses in staged reusable content; keep only fictional `@*.invalid` examples | Citation neutralized in writing-craft before push; owner-supplied private tokens were absent | Open — awaiting owner confirm to upgrade validators/checklist |
| AR-021 | Long-running network bake-off looked hung: no stdout and no output artifacts for 15+ minutes | Windows stdout buffering plus urllib3 DNS retries against an unresolvable host; no early progress file | Fail-fast connect/read timeouts; `python -u` / `flush=True`; create output dir + `progress.json` before the first network call | Network runners must preflight DNS, bound timeouts, and write a heartbeat artifact before external I/O | Bhashini client + MT bake-off now fail or proceed within seconds when DNS is broken | Resolved 2026-08-09 |
| AR-022 | Specialist API config succeeded but compute calls never connected (`getaddrinfo failed`) | Auth/config host and inference host differ; VPN broke system DNS for the compute hostname while DoH still resolved it | Discover `callbackUrl` from config; resolve via system DNS then DoH; pin IP for HTTPS; optional env IP override; never hard-code compute host as a silent bypass | Document auth≠compute readiness; add DoH/IP pin helper in the owning client asset | Bhashini GU→EN sample + 12/12 segments succeeded via DoH pin under India VPN | Resolved 2026-08-09 |
| AR-023 | `re.sub(pattern, findings, text)` crashed with `bad escape` when writing findings that contained Windows paths | Replacement string was parsed as a regex template; a backslash-prefixed path segment was interpreted as an invalid escape | Pass a callable replacer (`lambda _m: findings`) or escape backslashes before `re.sub` | Prefer callable replacements whenever inserting filesystem paths into regex updates | Benchmark doc §4 updated successfully after the fix | Resolved 2026-08-09 |
| AR-024 | Agents re-diagnosed the same network/key failures across sessions, burning tokens on hung bake-offs | Preflight existed only as tribal knowledge inside one project script | Promote a RA-004 bake-off skill + PREFLIGHT_GATE; RA-007 ships `preflight_indic.py`; consumers must hard-stop on non-zero before long loops | Inventory points agents at RA-004 skill first; long multi-provider runners call preflight and write `preflight.json` | Skill + guides + Indic preflight registered 2026-08-09 | Resolved 2026-08-09 |
| AR-025 | Central validation crashed before scanning, then default working-root validation reported fictional `.invalid` email and generated bytecode as publication defects | One case-insensitive regex flag was embedded inside a larger alternation; default and strict-clean boundaries were conflated; fictional subdomains were not recognized | Compile the whole credential pattern case-insensitively; reserve bytecode rejection for `--strict-clean`; accept any `.invalid` subdomain; keep personal-path examples generic | Smoke-test validator startup and both working-root and strict-clean modes whenever validation rules change | Tool smoke test and working-root library validation pass; strict-clean still rejects generated bytecode | Resolved 2026-08-09 |
| AR-026 | A trusted project-local PowerShell utility could not start because the managed Windows session disabled script execution | The reusable workflow assumed an executable path was sufficient, but the host execution policy blocked the script before its own validation ran | Invoke only the exact reviewed script in a new process with a process-scoped execution-policy bypass; never weaken the user or machine policy | Runtime preflight distinguishes missing tools from host policy blocks and documents the narrow process-scoped command | The approved local PostgreSQL manager completed without changing persistent policy | Resolved 2026-08-10 |
| AR-027 | A structurally valid email schema and fake-provider test treated a provider API message ID as though it were the RFC Internet Message-ID used by reply headers | Structural DBML validation proved types and relationships but could not detect semantic identifier conflation; the provider delivery ID and protocol correlation ID have different sources, scopes and uses | Add a distinct nullable Internet Message-ID field/index, keep mailbox + provider message ID for delivery deduplication, and correlate replies through provider thread then RFC reply identifiers | RA-001 now requires an explicit domain/provider/protocol identifier and uniqueness-scope review before accepting a generated model | RA-001 tests/build/typecheck pass; the consuming PostgreSQL schema migrates from zero and email reply/deduplication tests pass with distinct identifiers | Resolved 2026-08-10 |
| AR-028 | A provider adapter passed fake-provider tests but its first bounded live receipt completed the remote send and then failed post-send verification with `TypeError` | The fake Gmail request builder accepted `messageId`, while the real Gmail SDK requires `id`; the permissive double reproduced the application assumption instead of the SDK contract | Change both message fetch paths to `id`; keep the deterministic outbound Internet Message-ID so retry finds the already-sent message instead of sending twice | Provider-adapter doubles must enforce documented SDK parameter names, and authorization must be followed by a bounded live read/send smoke before declaring integration readiness | Focused adapter/auth tests passed; bounded retry reported `sent=1`, `failed=0`, and Gmail duplicate suppression recovered the already-sent receipt | Resolved 2026-08-10 |
| AR-029 | A change-feed batch stopped with resource-level HTTP 404 after three valid replies were listed | The provider history was valid, but one listed message disappeared before content retrieval; treating every 404 as checkpoint expiry conflated a per-resource tombstone with an invalid feed cursor | Skip only a 404 from individual resource retrieval, continue the bounded batch and advance to the provider-returned checkpoint; keep history-list 404 mapped to checkpoint recovery and rethrow other errors | History-adapter tests must cover list-to-fetch deletion races, expired checkpoints and non-404 failures as distinct cases | Consumer regression test passed; the retried live batch appended all three replies to one Case with no new Case or receipt | Resolved 2026-08-10 |
| AR-030 | A private comparative note mixed named source evidence with a consuming product's architecture and backlog | The research-intent gate controlled scope and stopping but did not control artifact topology; one file became both evidence and product specification | Keep named evidence in a designated private corpus and move only independently authored conclusions into existing product owners; preserve immutable archives separately | Require a two-corpus gate, minimal custody exceptions and deterministic scans for both forbidden intersections before handover | Active evidence corpus has no consumer identifiers; protected product and reusable roots have no named comparative details; asset and consumer checks pass | Resolved 2026-08-10 |
| AR-031 | A generated project failed its local-link audit because a copied template retained a relative link to the reusable-asset library | The source template was valid inside the library, but its path assumptions did not survive scaffolding into an unrelated project directory | Keep generated guidance self-contained; retain cross-asset pointers only in library-owned instructions and references | Run scaffold smoke tests from an isolated temporary directory and require every generated local link to resolve without the source library | RA-005 standard scaffold and full delivery-tool regression suite pass from a temporary directory | Resolved 2026-08-11 |
| AR-032 | A cross-agent planning handoff triggered unauthorized implementation, false security substitution, premature status claims and excess tool use | The handoff mixed planning and implementation language without an authoritative execution mode, exact approval token, write allowlist, budget or evidence-to-status rule; the asset manifest also named files that did not exist | Keep the receiver in `REVIEW_ONLY` by default; require a versioned control block, receipt and exact human authorization before bounded implementation; forbid unapproved mocks and fail closed on missing security prerequisites | RA-010 v1.2 adds execution modes, authority/budget envelopes, truthful status states, baseline/rollback gates, a real template, anonymized postmortem and three evaluation cases | Package structure and boundary checks pass; SOL plus one independent behavioral evaluation remain pending before promotion from Pilot | Open 2026-08-11 |
| AR-033 | “Self-healing” was proposed as automatically updating the factory's core prompt after a single defect | A local bug fix was conflated with a reusable-method change; silent mutation can overfit one incident, create regressions, widen authority and remove rollback | Stabilize the project first; record a versioned learning-change candidate; route it to the existing owner; add regression and different-project evaluations; require independent review, human disposition, staged rollout and rollback | RA-005 now provides a learning-change contract, RA-008 defines independent fresh-context review, and the inventory states that reusable-core mutation is never silent or autonomous | Delivery-tool tests, asset-tool smoke tests and library validation pass; cross-project behavioral proof remains required before promotion | Open 2026-08-13 |
| AR-034 | A platform-bundled skill creator and RA-003 were proposed as one copied “superskill” in a hosted registry | Platform-specific authoring guidance was confused with the canonical cross-project asset owner; copying both would create update drift and competing sources of truth | Keep RA-003 canonical, add a portable AI-skill authoring reference, and treat Codex or hosted-registry registrations as bounded distribution adapters | Require one canonical owner, adapter owner/version and refresh rules, plus three AI-skill behavioral evaluations before claiming cross-runtime readiness | Package structure and focused validation are part of v2.0; fresh cross-runtime behavioral evaluations remain pending | Open 2026-08-26 |

When the same root cause recurs, update the existing row and prevention rather than adding a duplicate issue.
